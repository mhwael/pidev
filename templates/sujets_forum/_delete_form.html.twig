<form method="post" action="{{ path('app_sujets_forum_delete', {'id': sujets_forum.id}) }}" class="d-inline" onsubmit="return confirm('Supprimer ce sujet et ses messages ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sujets_forum.id) }}">
    <button type="submit" class="btn_2 btn-sm" style="background:#dc3545;border-color:#dc3545;color:#fff;">Supprimer</button>
</form>
